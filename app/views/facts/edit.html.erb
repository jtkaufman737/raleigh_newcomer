<DOCTYPE html>
<head>
  <img src='raleigh_5.jpeg' style='position:fixed;top:0px;left:0px;width:100%;height:100%;z-index:-1;'>

</head>
<body>
<h3>Editing Facts</h3>

<%= form_for :fact, url: fact_path(@fact), method: :patch do |f| %>

<% if @fact.errors.any? %>
<div id='error_explanation'>
  <h6>
    <%= pluralize(@fact.errors.count, "error") %> prohibited this article from being saved:
  </h6>
  <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :category %>
    <%= f.text_field :category %>
  </p>
  <p>
    <%= f.label :text %>
    <%= f.text_area :body%>
  </p>
  <p>
    <%= f.label :source %>
    <%= f.text_area :source %>
  <p><div id="buttonStyle">
    <%= f.submit %>
  </p>
  <% end %>
  <%= link_to 'Back', facts_path %></div>
</body>
</html>
